# =============================================================================
# SAM (Sortir Au Maroc) - Configuration Environment
# =============================================================================
# INSTRUCTIONS:
# 1. Copier ce fichier vers .env : cp .env.example .env
# 2. Remplacer toutes les valeurs par vos propres credentials
# 3. NE JAMAIS committer le fichier .env
# =============================================================================

# -----------------------------------------------------------------------------
# SUPABASE
# -----------------------------------------------------------------------------
# URL de votre projet Supabase
VITE_SUPABASE_URL="https://your-project.supabase.co"

# Clé publique (anon key) - peut être exposée côté client
VITE_SUPABASE_ANON_KEY="your-supabase-anon-key"

# Clé publiable Supabase
VITE_SUPABASE_PUBLISHABLE_KEY="your-supabase-publishable-key"

# URL Supabase (pour le serveur)
SUPABASE_URL="https://your-project.supabase.co"

# ATTENTION: Clé de service - NE JAMAIS EXPOSER CÔTÉ CLIENT
# Générer depuis: Supabase Dashboard > Settings > API > service_role key
SUPABASE_SERVICE_ROLE_KEY="your-supabase-service-role-key"

# -----------------------------------------------------------------------------
# ADMIN DASHBOARD
# -----------------------------------------------------------------------------
# Clé API pour les endpoints admin (générer une clé sécurisée de 32+ caractères)
# Exemple: openssl rand -base64 32
ADMIN_API_KEY="generate-a-secure-api-key-here"

# Identifiants du dashboard admin
ADMIN_DASHBOARD_USERNAME="admin"
ADMIN_DASHBOARD_PASSWORD="change-this-secure-password"

# -----------------------------------------------------------------------------
# LACAISSEPAY (Paiement)
# -----------------------------------------------------------------------------
# URL du checkout LacaissePay
# DEV: https://paydev.lacaissepay.ma
# PROD: https://pay.lacaissepay.ma
VITE_LACAISSEPAY_CHECKOUT_URL="https://paydev.lacaissepay.ma"

# URL de création de session LacaissePay
# DEV: https://sessiondev.lacaissepay.ma/storePaymentData
# PROD: https://session.lacaissepay.ma/storePaymentData
LACAISSEPAY_SESSION_URL="https://sessiondev.lacaissepay.ma/storePaymentData"

# DEV ONLY: Numéro de téléphone pour les tests
LACAISSEPAY_DEV_PHONE="+212600000000"

# -----------------------------------------------------------------------------
# EMAIL (SMTP)
# -----------------------------------------------------------------------------
SMTP_HOST="smtp.example.com"
SMTP_PORT="587"
SMTP_USER="your-smtp-username"
SMTP_PASS="your-smtp-password"
SMTP_SECURE="false"

# Informations d'expéditeur
EMAIL_FROM_NAME="Sortir Au Maroc"
EMAIL_DOMAIN="sortiraumaroc.ma"

# -----------------------------------------------------------------------------
# APPLICATION
# -----------------------------------------------------------------------------
# URL publique de l'application
PUBLIC_BASE_URL="https://sam.ma"

# Environnement: development | production
NODE_ENV="development"

# Builder.io (CMS)
VITE_PUBLIC_BUILDER_KEY="your-builder-io-key"

# Message de test
PING_MESSAGE="ping pong"

# -----------------------------------------------------------------------------
# FIREBASE (Authentification par téléphone)
# -----------------------------------------------------------------------------
# Configuration Firebase - Créer un projet sur https://console.firebase.google.com
# Activer l'authentification par téléphone dans Authentication > Sign-in method

# Clé API Firebase (publique)
VITE_FIREBASE_API_KEY="your-firebase-api-key"

# Domaine d'authentification Firebase
VITE_FIREBASE_AUTH_DOMAIN="your-project.firebaseapp.com"

# ID du projet Firebase
VITE_FIREBASE_PROJECT_ID="your-firebase-project-id"

# Bucket de stockage Firebase (optionnel pour l'auth par téléphone)
VITE_FIREBASE_STORAGE_BUCKET="your-project.appspot.com"

# ID de l'expéditeur de messages Firebase
VITE_FIREBASE_MESSAGING_SENDER_ID="your-messaging-sender-id"

# ID de l'application Firebase
VITE_FIREBASE_APP_ID="your-firebase-app-id"

# ID du projet Firebase pour le serveur (vérification des tokens)
FIREBASE_PROJECT_ID="your-firebase-project-id"

# Clé VAPID pour les notifications push web
# Générer depuis: Firebase Console > Project Settings > Cloud Messaging > Web Push certificates
VITE_FIREBASE_VAPID_KEY="your-firebase-vapid-key"

# Service Account JSON pour Firebase Admin SDK (notifications push serveur)
# Générer depuis: Firebase Console > Project Settings > Service accounts > Generate new private key
# Copier le contenu JSON complet sur une seule ligne
FIREBASE_SERVICE_ACCOUNT_JSON='{"type":"service_account","project_id":"...","private_key":"...","client_email":"..."}'

# -----------------------------------------------------------------------------
# SÉCURITÉ - WEBHOOKS
# -----------------------------------------------------------------------------
# Clé secrète pour sécuriser les webhooks de paiement
# IMPORTANT: Générer une clé forte de 32 caractères minimum
# Exemple: openssl rand -hex 32
PAYMENTS_WEBHOOK_KEY="generate-a-secure-webhook-key-here"

# -----------------------------------------------------------------------------
# MONITORING - SENTRY
# -----------------------------------------------------------------------------
# DSN Sentry pour le monitoring des erreurs serveur
# Obtenir depuis: https://sentry.io > Votre projet > Settings > Client Keys
SENTRY_DSN="https://xxxx@xxxx.ingest.sentry.io/xxxx"

# DSN Sentry pour le client (peut être le même ou différent)
VITE_SENTRY_DSN="https://xxxx@xxxx.ingest.sentry.io/xxxx"

# Version de l'application (pour les releases Sentry)
VITE_APP_VERSION="1.0.0"

# -----------------------------------------------------------------------------
# ANTHROPIC (Assistant IA pour la rédaction)
# -----------------------------------------------------------------------------
# Clé API Anthropic pour l'assistant IA de rédaction d'articles
# Obtenir depuis: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY="your-anthropic-api-key"

# -----------------------------------------------------------------------------
# CRON JOBS
# -----------------------------------------------------------------------------
# Clé secrète pour sécuriser les endpoints cron
# IMPORTANT: Générer une clé forte de 32 caractères minimum
# Exemple: openssl rand -hex 32
CRON_SECRET="generate-a-secure-cron-secret-here"
